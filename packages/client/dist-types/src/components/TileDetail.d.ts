import { type GameState } from '@rentier/shared';
interface Props {
    state: GameState;
    tileId: number;
    onClose: () => void;
}
export default function TileDetail({ state, tileId, onClose }: Props): import("react").JSX.Element | null;
export {};
//# sourceMappingURL=TileDetail.d.ts.map